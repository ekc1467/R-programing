총알 예시
결과 보고

crtl+ent 한 줄

drag crtl+ent 

sqrt/m=20 파라미터일 때만 권장
%%

run

cat

alt - 는 화살표

옵션

R은 저장되면 벡터

2+3은 2라는 벡터와 3이라는 벡터의 합

floating point

double

typeof는 x의 원소의 유형을 리턴(우선 순위에 따라 타입 값이 나옴. 즉 자동으로 트루는 숫자 1로 바뀌거나 문자로 바귐.통계 프로그래밍이라)

v5<-seq(1,101,3)

배열을 저장하고 배열이름[숫자]면 그 숫자를 표현

each

서로 다른 크기의 배열을 더하면?

함수 c<-c(1:100),seq

항상 변수 값에 넣어준다. 반복 사용할 수 있으니

?기능


점수가 이제 등록한 것이 장학금을 받으면 없어지지는 않을 것이다.
마일리지를 sw에 적용하면 지역선도에 사용하지 못하는가?
먼저 신청을 해버렸는데...제일 먼저한 걸 제일 늦게 신청했다.
그 기간 사이에 신청을 하는 것인가 아니면 그 기간안에 수업도 들어야하는 것인가?

2,3번

종속변수는 하나일 수 밖에없다
