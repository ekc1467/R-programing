벡터로 모든 것을 만든다.
벡터의 디피컬은 로지컬.
length=2로 2개를 만들려고 하면 2개의 펄스 로지컬을 준다.
원소를 뉴메릭(숫자)로 넣으면 벡터는 동일한 유형을 가져야하므로 트루는 1 펄스는 0로 같이 바뀐다.
물론 캐릭터 타입을 넣으면 캐릭터타입으로 다 바뀐다.
팩터는 분류 개수로 치기때문에 B가 2개면 하나의 B만 인정하는 등 그렇다.
리스트는 서로 다른 유형을 가질 수 있는 벡터이기 때문에 컬럼(이름) 그리고 값으로 되어있다.($컬럼 이름으로 접근가능)
매트릭스는 비어있으면 NA로 채운다.
길이가 다른 것을 서로 더하면 짧은 것을 반복시켜 더함.
함수형 프로그래밍인 R에서는 반목문과 조건문을 굳이 쓰지마라.#ifelse를사용해라
map,filter,reduce,apply
apply는 매개변수로 함수를 던져준다. map과 연계
dplyr도 깔끔하게 사용가능.
빅데이터 3V
클라우드 컴퓨팅의 시대가 왔다.
지도학습
비지도학습